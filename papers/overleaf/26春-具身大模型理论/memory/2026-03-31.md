- 启动了 TableGPT2-7B 的 Ollama 部署流程，确认了从 HuggingFace 到 GGUF 格式的转换必要性。
- 诊断出 Windows 路径包含中文和空格是导致脚本执行失败的主要原因，建议用户将项目目录重命名为 `model_theory`。
- 制定了使用 ModelScope 镜像下载模型并结合 llama.cpp 进行转换的替代方案，以解决网络连接超时问题。
- 用户尝试使用 `set location` 命令切换目录失败，已纠正为在 CMD 中使用 `cd /d` 或在 PowerShell 中使用 `Set-Location`。- 诊断出 HuggingFace 下载速度极慢的原因为未设置 HF_TOKEN 导致限速，确认 ModelScope 为国内最优下载源。
- 用户咨询如何在 HuggingFace 设置 HF_TOKEN，已提供图形界面和命令行两种永久配置方案。
- 用户提供了 HuggingFace Token，已成功通过 PowerShell 命令将其永久写入 Windows 用户环境变量 `HF_TOKEN`，解决了下载限速问题。
