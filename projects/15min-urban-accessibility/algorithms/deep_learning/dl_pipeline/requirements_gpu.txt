# ============================================================
# 深度学习 Pipeline GPU 依赖
# ============================================================
# 安装: pip install -r requirements_gpu.txt
# 推荐: RTX 5090 (32GB) / RTX 4090 (24GB) / RTX A6000 (48GB)
# CPU 可运行但速度较慢
# ============================================================

# --- PyTorch (GPU 支持) ---
# RTX 30/40/50 系列用此版本
# --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# --- HuggingFace Transformers ---
transformers>=4.36.0
accelerate>=0.25.0
sentencepiece>=0.1.99
tokenizers>=0.15.0

# --- 分割模型 ---
segmentation-models-pytorch>=0.3.3  # U-Net/DeepLabV3+ 等统一接口
pretrainedmodels>=0.7.4             # 预训练模型
resmem>=0.1.0                       # 残差记忆

# --- YOLO (目标检测) ---
ultralytics>=8.1.0                  # YOLOv8 (walkability perception)

# --- 数据处理 ---
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0                 # 分层采样
scipy>=1.11.0
Pillow>=10.0.0
opencv-python>=4.8.0
opencv-python-headless>=4.8.0      # 无GUI服务器环境
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# --- 可视化 ---
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0

# --- 网络请求 ---
requests>=2.31.0
urllib3>=2.0.0

# --- 进度条 ---
tqdm>=4.66.0

# --- 配置文件 ---
pyyaml>=6.0.1
toml>=0.10.2

# --- GPU 信息 ---
nvidia-ml-py3>=7.352.0               # NVML GPU 监控

# --- CPU 优化 ---
# MKL (Intel) 或 OpenBLAS
# conda install mkl mkl-include  # 推荐 conda 安装
# 或 pip install mkl mkl-include

# --- 开发工具 ---
pytest>=7.4.0                       # 单元测试
black>=23.0.0                       # 代码格式化
isort>=5.12.0                       # import 排序
flake8>=6.1.0                       # lint

# ============================================================
# 快速安装命令 (PowerShell)
# ============================================================
# 1. 先装 PyTorch (GPU版本)
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# 2. 再装其余依赖
#    pip install transformers accelerate ultralytics
#    pip install segmentation-models-pytorch
#    pip install numpy pandas scikit-learn opencv-python-headless
#    pip install matplotlib seaborn pillow requests tqdm pyyaml
#    pip install nvidia-ml-py3
#
# 3. 验证安装
#    python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"CPU\"}')"
#
# 4. 验证分割模型
#    python -c "import segmentation_models_pytorch as smp; print(f'smp: {smp.Unet}')"
#
# ============================================================
